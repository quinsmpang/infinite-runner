/* * Playdom, Inc. (c)2013 All Rights Reserved */ package com.playdom.common.interfaces  {    import flash.display.BitmapData;	/**	 * Provides access to images.  	 * 	 * @author Rob Harris	 */	public interface IBitmaps	{		/**		 * Fetches a BitmapData object.		 *  		 * @param key       A key to associate with the image.		 * @param filename  The file name (optional) for on-demand images.		 * @param listener  The function to be called when an on-demand image is ready.		 * 		 * @return A BitmapData object or null if not found (or not loaded yet). 		 */		function getBitmapData(key:String, filename:String=null, listener:Function=null):BitmapData;		/**		 * Stores a BitmapData object.		 *  		 * @param key    A key to associate with the image.		 * @param image  The image to be stored. 		 */		function putBitmapData(key:String, image:BitmapData):void;	}}